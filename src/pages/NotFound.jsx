import { Container, Text } from "@radix-ui/themes";
import React from "react";

export default function NotFound() {
	return (
		<Container>
			<Text>You can use this page to inform users when enter invalid path</Text>
		</Container>
	);
}
